pipeline {
    agent any 

    stages {
        stage("clone") {
            steps {
                sh 'echo Cloning'
            }
        }
        stage("build") {
            steps 'echo Building'
        }
    }
}